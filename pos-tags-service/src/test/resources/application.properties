# Rest clients
# The spacy service must be started before the integration tests
spacy-api/mp-rest/url=http://localhost:8888/dep

# datasource configuration
# The H2 database is started by the tests
quarkus.datasource.url=jdbc:h2:mem:postags
quarkus.datasource.driver=org.h2.Driver
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true
